# =============================================================================
# NON-AUTHORITATIVE convenience mirror of the project's DIRECT dependencies.
#
# The authoritative dependency spec is pyproject.toml (resolved into uv.lock).
# Install the project with:   uv sync
# (NOT `pip install -r requirements.txt` — that won't fetch the bundled CARLA
#  wheel, the mmrphys git dependency, or the CPU-pinned torch build.)
#
# Constraints here are intentionally LOOSE (>=) so newer releases are allowed.
# A few caps are load-bearing and MUST stay — see the inline notes.
# =============================================================================

# carla: installed from the bundled Windows wheel in wheels/ (or your CARLA
# PythonAPI), not from PyPI:
#   carla>=0.10.0

dash>=3.3.0
dash-bootstrap-components>=2.0.4
flask>=3.1.2
matplotlib>=3.10.7

# mediapipe: releases >=0.10.22 have no working Windows cp312 wheel (the
# artifact is missing the mediapipe.python bindings, so `mp.solutions` is gone).
mediapipe>=0.10.14,<0.10.22
# protobuf: mediapipe's bundled generated code needs the protobuf 4.x runtime;
# protobuf 5/6/7 break `mediapipe.solutions` at runtime.
protobuf>=4.25.3,<5

numpy>=1.26.4
huggingface-hub>=0.25.0
pandas>=2.3.3
pyyaml>=6.0.3
scipy>=1.15.3
seaborn>=0.13.2

# torch/torchvision: PyPI ships the CPU build on Windows. For the CUDA build
# (GPU training) use scripts/setup_cuda_torch.py — see pyproject.toml notes.
# NOTE: we depend on a fork of mmrphys (M-Colley/MMRPhys) that loosens its
# torch/torchvision pins, so newer torch (2.12) resolves normally — no override.
torch>=2.12.0
torchvision>=0.27.0

tqdm>=4.67.1
pygame>=2.6.1
ultralytics>=8.4.57
keras>=3.12.0
pandas-stubs>=2.3.3
joblib>=1.5.2

# scikit-learn: pinned — the committed trained_models/*.pkl are sklearn pickles
# and are version-sensitive. Bump only after retraining them.
scikit-learn==1.7.0

pyttsx3>=2.99
fastapi>=0.124.4
python-socketio>=5.15.0
uvicorn>=0.38.0
xgboost>=3.2.0

# xlstm: pinned to the validated CPU-only mLSTM xLSTMBlockStack build.
xlstm==2.0.5
einops>=0.8
dacite>=1.8
omegaconf>=2.3

# mmrphys: git dependency (not on PyPI) — our fork with loosened torch pins:
#   mmrphys @ git+https://github.com/M-Colley/MMRPhys@93d3833262646ba8d5a4a3d82f5f85d159677e26

# NOTE: `asyncio` is intentionally absent — it is part of the Python standard
# library; the same-named PyPI package is an abandoned backport that shadows it.
